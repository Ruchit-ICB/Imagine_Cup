{
  "installCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b stable ~/.flutter && ~/.flutter/bin/flutter config --no-analytics && ~/.flutter/bin/flutter pub get",
  "buildCommand": "~/.flutter/bin/flutter build web",
  "outputDirectory": "build/web",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
